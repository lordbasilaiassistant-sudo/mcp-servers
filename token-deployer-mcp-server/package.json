{
  "name": "@thryx/token-deployer-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for deploying and managing tokens on Base via OBSD LaunchPad — launch tokens, trade, claim fees, security scan, trending tokens",
  "main": "dist/index.js",
  "bin": {
    "token-deployer-mcp-server": "dist/index.js"
  },
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "lint": "npx tsc --noEmit"
  },
  "keywords": ["mcp", "model-context-protocol", "token", "deployer", "base", "obsd", "launchpad", "erc20", "ai-agent"],
  "author": "Anthony <drlordbasil@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "ethers": "^6.16.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^3.0.0"
  }
}
